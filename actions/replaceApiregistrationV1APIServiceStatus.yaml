name: replaceApiregistrationV1APIServiceStatus
pack: kubernetes
runner_type: python-script
description: "replace status of the specified APIService"
enabled: true
entry_point: "replaceApiregistrationV1APIServiceStatus.py"
parameters:
    config_override:
        type: object
        description: "override stackstorm config"
        secret: true
    body:
        type: object
        description: ""
        required: True
        
    name:
        type: string
        description: "name of the APIService"
        required: True
        
    pretty:
        type: string
        description: "If 'true', then the output is pretty printed."
        