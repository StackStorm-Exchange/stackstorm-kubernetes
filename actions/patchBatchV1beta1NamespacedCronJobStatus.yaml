name: patchBatchV1beta1NamespacedCronJobStatus
pack: kubernetes
runner_type: python-script
description: "partially update status of the specified CronJob"
enabled: true
entry_point: "patchBatchV1beta1NamespacedCronJobStatus.py"
parameters:
    config_override:
        type: object
        description: "override stackstorm config"
        secret: true
    body:
        type: object
        description: ""
        required: True
        
    name:
        type: string
        description: "name of the CronJob"
        required: True
        
    namespace:
        type: string
        description: "object name and auth scope, such as for teams and projects"
        required: True
        
    pretty:
        type: string
        description: "If 'true', then the output is pretty printed."
        