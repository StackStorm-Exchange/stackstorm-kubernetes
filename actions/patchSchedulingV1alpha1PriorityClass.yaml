name: patchSchedulingV1alpha1PriorityClass
pack: kubernetes
runner_type: python-script
description: "partially update the specified PriorityClass"
enabled: true
entry_point: "patchSchedulingV1alpha1PriorityClass.py"
parameters:
    config_override:
        type: object
        description: "override stackstorm config"
        secret: true
    body:
        type: object
        description: ""
        required: True
        
    name:
        type: string
        description: "name of the PriorityClass"
        required: True
        
    pretty:
        type: string
        description: "If 'true', then the output is pretty printed."
        