---
  class_name: "StatefulsetsResource"
  entry_point: "statefulsets_resource.py"
  description: "Sensor that watches Kubernetes API for new Statefulsets objects."
  trigger_types:
    -
      name: "statefulsets"
      description: "Trigger for statefulsets event dispatch."
      payload_schema:
        type: "object"
        properties:
          resource:
            type: "obj"
          name:
            type: "obj"
          labels:
            type: "obj"
          object_kind:
            type: "obj"
          namespace:
            type: "obj"
