---
  name: "create_tpr_sensor"
  runner_type: "python-script"
  description: "Create new 3PR sensor"
  enabled: true
  entry_point: "create_tpr_sensor.py"
  pack: "kubernetes"
  parameters:
    payload:
      type: "object"
      description: "payload object from kubernetes sensor"
