name: createAdmissionregistrationV1beta1ValidatingWebhookConfiguration
pack: kubernetes
runner_type: python-script
description: "create a ValidatingWebhookConfiguration"
enabled: true
entry_point: "createAdmissionregistrationV1beta1ValidatingWebhookConfiguration.py"
parameters:
    config_override:
        type: object
        description: "override stackstorm config"
        secret: true
    body:
        type: object
        description: ""
        required: True
        
    pretty:
        type: string
        description: "If 'true', then the output is pretty printed."
        