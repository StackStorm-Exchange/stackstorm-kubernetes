---
  name: "elasticsearch_spec"
  entry_point: "elasticsearch_spec.py"
  pack: "kubernetes"
  description: "Applies elasticsearch access policies"
  enabled: true
  runner_type: run-python
  parameters:
    payload:
      type: "object"
      description: "kubernetes payload object, not required in stackstorm Web UI"
    source_ip:
      type: "array"
      description: "list of white-listed IP addresses for elasticsearch service"

