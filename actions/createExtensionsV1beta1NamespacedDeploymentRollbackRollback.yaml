name: createExtensionsV1beta1NamespacedDeploymentRollbackRollback
pack: kubernetes
runner_type: run-python
description: "create rollback of a DeploymentRollback"
enabled: true
entry_point: "createExtensionsV1beta1NamespacedDeploymentRollbackRollback.py"
parameters:
    body:
        type: object
        description: ""
        required: True
    name:
        type: string
        description: "name of the DeploymentRollback"
        required: True
    namespace:
        type: string
        description: "object name and auth scope, such as for teams and projects"
        required: True
    config_override:
        type: object
        description: "override stackstorm config"
    pretty:
        type: string
        description: "If 'true', then the output is pretty printed."
