name: patchCertificatesV1alpha1CertificateSigningRequest
pack: kubernetes
runner_type: run-python
description: "partially update the specified CertificateSigningRequest"
enabled: true
entry_point: "patchCertificatesV1alpha1CertificateSigningRequest.py"
parameters:
    body:
        type: object
        description: ""
        required: True
    name:
        type: string
        description: "name of the CertificateSigningRequest"
        required: True
    config_override:
        type: object
        description: "override stackstorm config"
    pretty:
        type: string
        description: "If 'true', then the output is pretty printed."
