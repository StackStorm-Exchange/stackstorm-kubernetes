name: createCertificatesV1alpha1CertificateSigningRequest
pack: kubernetes
runner_type: run-python
description: "create a CertificateSigningRequest"
enabled: true
entry_point: "createCertificatesV1alpha1CertificateSigningRequest.py"
parameters:
    body:
        type: object
        description: ""
        required: True
    config_override:
        type: object
        description: "override stackstorm config"
    pretty:
        type: string
        description: "If 'true', then the output is pretty printed."
