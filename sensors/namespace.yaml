---
  class_name: "Namespace"
  entry_point: "namespace.py"
  description: "Sensor which watches Kubernetes API for new namespaces."
  trigger_types:
    -
      name: "namespace"
      description: "Trigger which contains metadata for namespace dispatch."
      payload_schema:
        type: "object"
        properties:
          resource:
            type: "obj"
          name:
            type: "obj"
          labels:
            type: "obj"
          object_kind:
            type: "obj"
