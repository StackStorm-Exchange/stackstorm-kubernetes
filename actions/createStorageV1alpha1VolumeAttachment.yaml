name: createStorageV1alpha1VolumeAttachment
pack: kubernetes
runner_type: python-script
description: "create a VolumeAttachment"
enabled: true
entry_point: "createStorageV1alpha1VolumeAttachment.py"
parameters:
    config_override:
        type: object
        description: "override stackstorm config"
        secret: true
    body:
        type: object
        description: ""
        required: True
        
    pretty:
        type: string
        description: "If 'true', then the output is pretty printed."
        