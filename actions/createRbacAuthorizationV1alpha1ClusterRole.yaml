name: createRbacAuthorizationV1alpha1ClusterRole
pack: kubernetes
runner_type: python-script
description: "create a ClusterRole"
enabled: true
entry_point: "createRbacAuthorizationV1alpha1ClusterRole.py"
parameters:
    config_override:
        type: object
        description: "override stackstorm config"
        secret: true
    body:
        type: object
        description: ""
        required: True
        
    pretty:
        type: string
        description: "If 'true', then the output is pretty printed."
        