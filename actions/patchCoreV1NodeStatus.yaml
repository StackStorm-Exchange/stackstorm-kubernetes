name: patchCoreV1NodeStatus
pack: kubernetes
runner_type: run-python
description: "partially update status of the specified Node"
enabled: true
entry_point: "patchCoreV1NodeStatus.py"
parameters:
    body:
        type: string
        description: ""
        required: True
    name:
        type: string
        description: "name of the Node"
        required: True
    pretty:
        type: string
        description: "If 'true', then the output is pretty printed."
