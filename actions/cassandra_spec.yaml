---
  name: "cassandra_spec"
  entry_point: "cassandra_spec.py"
  pack: "kubernetes"
  description: "Munge Kubernetes data so we can create a Cassandra Cloudformation Stack."
  runner_type: run-python
  parameters:
    payload:
      type: object
    config:
      type: object
