name: createPolicyV1beta1PodSecurityPolicy
pack: kubernetes
runner_type: python-script
description: "create a PodSecurityPolicy"
enabled: true
entry_point: "createPolicyV1beta1PodSecurityPolicy.py"
parameters:
    config_override:
        type: object
        description: "override stackstorm config"
        secret: true
    body:
        type: object
        description: ""
        required: True
        
    pretty:
        type: string
        description: "If 'true', then the output is pretty printed."
        