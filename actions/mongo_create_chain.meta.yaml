---
  name: "mongo_create_chain"
  runner_type: "action-chain"
  description: "Mongo cloudformation Create Stack Action Chain Workflow"
  enabled: true
  entry_point: "workflows/mongo_create_chain.yaml"
  pack: "kubernetes"
  parameters:
    payload:
      type: "object"
      description: "payload object from kubernetes sensor"
    cmd:
      type: "string"
      description: "payload object from kubernetes sensor"
