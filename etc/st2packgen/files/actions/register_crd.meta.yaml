---
  name: "register_crd"
  runner_type: ""mistral-v2"
  description: "Create a new st2 sensor and load it"
  enabled: true
  entry_point: "workflows/register_crd.yaml"
  pack: "kubernetes"
  parameters:
    payload:
      type: "object"
      description: "payload object from kubernetes sensor"
